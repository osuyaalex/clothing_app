import 'package:flutter/material.dart';
class WomenBagWork extends StatelessWidget {

  const WomenBagWork({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
      ),
    );
  }
}