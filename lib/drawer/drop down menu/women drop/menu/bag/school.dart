import 'package:flutter/material.dart';
class WomenBagSchool extends StatelessWidget {

  const WomenBagSchool({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
      ),
    );
  }
}