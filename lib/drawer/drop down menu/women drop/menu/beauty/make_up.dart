import 'package:flutter/material.dart';

class WomenBeautyMakeUp extends StatelessWidget {
  const WomenBeautyMakeUp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
