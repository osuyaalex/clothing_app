import 'package:flutter/material.dart';
class WomanApparelDenim extends StatelessWidget {
  const WomanApparelDenim({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}