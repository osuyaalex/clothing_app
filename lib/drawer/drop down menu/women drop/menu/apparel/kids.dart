import 'package:flutter/material.dart';
class WomanApparelKids extends StatelessWidget {
  const WomanApparelKids({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
