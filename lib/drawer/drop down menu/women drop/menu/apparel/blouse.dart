import 'package:flutter/material.dart';
class WomanApparelBlouse extends StatelessWidget {
  const WomanApparelBlouse({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}