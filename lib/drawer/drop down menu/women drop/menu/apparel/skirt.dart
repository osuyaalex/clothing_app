import 'package:flutter/material.dart';
class WomanApparelSkirt extends StatelessWidget {
  const WomanApparelSkirt({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}