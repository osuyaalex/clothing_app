import 'package:flutter/material.dart';
class WomanApparelOuter extends StatelessWidget {
  const WomanApparelOuter({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}