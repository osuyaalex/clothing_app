import 'package:flutter/material.dart';
class WomanApparelPants extends StatelessWidget {
  const WomanApparelPants({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}