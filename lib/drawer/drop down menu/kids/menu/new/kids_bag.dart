import 'package:flutter/material.dart';

class KidsBag extends StatelessWidget {
  const KidsBag({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
