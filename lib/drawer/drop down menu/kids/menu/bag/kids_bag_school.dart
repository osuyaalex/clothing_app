import 'package:flutter/material.dart';

class KidsBagSchool extends StatelessWidget {
  const KidsBagSchool({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
