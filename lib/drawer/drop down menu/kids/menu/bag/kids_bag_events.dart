import 'package:flutter/material.dart';

class KidsBagEvents extends StatelessWidget {
  const KidsBagEvents({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
