import 'package:flutter/material.dart';

class KidsBeautyMakeUp extends StatelessWidget {
  const KidsBeautyMakeUp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
